using System;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {
            int ammoShot;
            uint countBerries;
            long lengthFlashlightBeam;
            ulong lengthTrack = 200;
            byte memoryOccupied;
            sbyte pointsScored;
            short numberOpponents;
            ushort wordsText;
            float healthBox;
            string tableNumber2Text = "здесь могла быть ваша реклама"; //2 означала номер таблички, например на уровне (табличка-номер2-текс)
            double pressureTire;
            bool isShot = true;
            char denial = '!';
        }
    }
}
